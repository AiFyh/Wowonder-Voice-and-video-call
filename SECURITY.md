# 安全说明

## 设计上的安全考虑

* **信令服务不暴露公网**：默认只监听 `127.0.0.1:9000`，由站点 Web 服务器反代；
  `allow_discovery=false` 禁止列举在线 peer id，避免被扫描滥用。
* **TURN 用临时账号**：coturn 开启 `use-auth-secret`，服务端用共享密钥按时签发
  `username = 到期时间戳:标识`、`credential = base64(HMAC-SHA1(username, secret))`，
  有效期 10 分钟；配置里**不存长期用户密码**。
* **密钥不进代码库**：`xhr/p2p_turn.php` 只从服务器文件读取密钥，任何情况下不输出、不写日志；
  本仓库所有密钥位置都是占位符。
* **请求受主程序约束**：所有 AJAX 都走主程序 `requests.php`，需要登录态 + `Wo_CheckMainSession($hash_id)`；
  `xhr/call_log.php` 另外校验 `to_id` 合法性（用户存在、不等于自己）与时长范围（1 秒 ~ 24 小时）。
* **不泄露源站 IP**：ICE 列表由服务端动态生成，前端不写死任何服务器地址；开源版本把兜底 STUN 也改成了配置项。

## 部署时请注意

1. **不要**把信令服务的 `PEERJS_HOST` 改成 `0.0.0.0`，除非你明确要把信令直接暴露到公网（无 TLS、无鉴权）；
2. **不要**把 `/etc/turn-rest-secret`、coturn 配置或站点 `config.php` 提交到任何仓库；
3. coturn 的 `static-auth-secret` 泄露＝任何人都能用你的服务器当中继，请按服务器密钥对待（定期轮换）；
4. 源站在 CDN 之后时，注意 `external-ip` / `p2p_stun_fallback` 等字段不要公开写进仓库或前端；
5. 建议给信令服务与 coturn 加基础监控（进程存活、UDP 端口可达），通话质量问题大多源自中继端口未放行。

## 报告安全问题

请通过项目 issue 的**私密渠道**（或仓库维护者的邮箱）报告，不要在公开 issue 里贴可利用细节与真实凭据。
请附：受影响版本/文件、复现步骤、影响面判断、以及你建议的修复方向。
维护者会在确认后尽快修复并在 `CHANGELOG.md` 里说明。

## 不在范围内

* WowWonder 主程序或 Sean 主题本身的漏洞（请向各自厂商报告）；
* WebRTC 协议栈 / 浏览器本身的实现问题；
* 未按本文档要求配置（例如把 9000 端口直接暴露到公网）导致的滥用。
